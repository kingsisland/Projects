#include <bits/stdc++.h>
using namespace std;

int main()
{
	#ifndef ONLINE_JUDGE
		freopen("input.txt", "r", stdin);

		fropen("output.txt", "w", stdout)
	#endif

	int n;
	cin>>n;
	cout<<n;
	return 0;
}